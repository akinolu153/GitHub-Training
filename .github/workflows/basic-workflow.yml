name: Basic Gitworkflow

on:
  workflow_dispatch:
    inputs:
      name:
        #descrptive name to show on UI
        description: "Please enter your name"
        #default: "Name"
        # Required to run
        required: true
        # input type (string, choice, boolean)
        type: string
      city:
        description: "Please select your state"
        required: true
        #default: "Choose City"
        type: choice
        options:
          - New Jersey
          - Georgia
          - Washington DC
          - New York
      color:
        description: "Is your Favorite color Blue"
        required: true
        type: boolean
        
jobs:
      # job name to be displayed in runtime
      Greeting_job:
        #display name
        name: "Greeting, Program"

        # runner agent to run the job
        runs-on: ubuntu-latest
        steps:
          - name: checkout code
            uses: actions/checkout@v4
           #runs command using runner shell
          - name: send greetings
            run: |
                echo "Hello ${{github.event.inputs.name}}"
                echo "Your state of Origin is ${{github.event.inputs.city}}"
                echo "Favorite Color is Blue? ${{github.event.inputs.color}}"
              
   
git branch